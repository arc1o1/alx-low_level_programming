#include <stdio.h>
/**
 * main - Entry point
 * @void: no paramater
 *
 * Description: Uses puts to output data
 *
 * "Programming is like building a multilingual puzzle
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char *ctrword = "\"Programming is like building a multilingual puzzle";

	puts(ctrword);
	return (0);
}
